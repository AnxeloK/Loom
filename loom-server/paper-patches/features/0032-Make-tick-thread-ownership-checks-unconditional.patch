From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: anxelok <anxelok@localhost>
Date: Sat, 20 Jun 2026 18:11:21 +0000
Subject: [PATCH] Make tick-thread ownership checks unconditional

The region-aware ownership checks were gated behind loom.parallelWorlds /
loom.parallelChunkTick, which defaulted off here while the engine ticked in
parallel by default - so the cross-region guard could be inactive on a default
server. Parallel ticking is now always on, so the checks are too: a tick thread
is valid only for the world and region it owns, and the global owner is always
accepted.

diff --git a/src/main/java/ca/spottedleaf/moonrise/common/util/TickThread.java b/src/main/java/ca/spottedleaf/moonrise/common/util/TickThread.java
index 50b7f94d7e172c825d69b2e810df4b5b9aecc5c9..2c36922658ff18555c4f3dcbbf027501b49a7435 100644
--- a/src/main/java/ca/spottedleaf/moonrise/common/util/TickThread.java
+++ b/src/main/java/ca/spottedleaf/moonrise/common/util/TickThread.java
@@ -136,15 +136,11 @@ public class TickThread extends Thread {
     }
 
     // Loom start - region-aware tick-thread ownership checks
-    // With parallel execution disabled (the default) these behave exactly as before: any tick thread is
-    // valid for any world. With parallel WORLD or parallel CHUNK ticking enabled, a thread is only valid
-    // for the world/region it actually owns, so cross-world or cross-region access off the owning thread is
-    // detected as a loud throw instead of silently corrupting another region's state. The position-aware
-    // overloads check CHUNK ownership (region granularity); the level-only overload checks the world.
-    private static final boolean PARALLEL_WORLDS = Boolean.getBoolean("loom.parallelWorlds");
-    private static final boolean PARALLEL_CHUNK_TICK = Boolean.getBoolean("loom.parallelChunkTick");
-    private static final boolean PARALLEL_EXECUTION = PARALLEL_WORLDS || PARALLEL_CHUNK_TICK;
-
+    // Worlds and intra-world regions tick in parallel, so a tick thread is only valid for the world/region it
+    // actually owns; cross-world or cross-region access off the owning thread is detected as a loud throw
+    // instead of silently corrupting another region's state. The position-aware overloads check CHUNK
+    // ownership (region granularity); the level-only overload checks the world. The global owner (main thread)
+    // is always accepted.
     private static boolean ownsLevel(final Level world) {
         if (!(world instanceof final net.minecraft.server.level.ServerLevel level)) {
             return true;
@@ -162,16 +158,10 @@ public class TickThread extends Thread {
     }
 
     private static boolean isTickThreadForLevel(final Level world) {
-        if (!PARALLEL_EXECUTION) {
-            return isTickThread();
-        }
         return isTickThread() && ownsLevel(world);
     }
 
     private static boolean isTickThreadForChunk(final Level world, final int chunkX, final int chunkZ) {
-        if (!PARALLEL_EXECUTION) {
-            return isTickThread();
-        }
         return isTickThread() && ownsChunk(world, chunkX, chunkZ);
     }
 
@@ -220,9 +210,6 @@ public class TickThread extends Thread {
     }
 
     public static boolean isTickThreadFor(final Entity entity) {
-        if (!PARALLEL_EXECUTION) {
-            return isTickThread();
-        }
         if (!isTickThread()) {
             return false;
         }
