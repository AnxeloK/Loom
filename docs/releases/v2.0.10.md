# Loom 2.0.10

This release reduces region scheduler allocation by using primitive bucket keys for region tasks, scheduled tasks, and cross-region messages while preserving deterministic dispatch order.
